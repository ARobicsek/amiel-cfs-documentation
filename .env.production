# Production environment variables
# This file is used when building for production (Vercel)

# DO NOT SET VITE_API_URL - we want relative URLs in production
# This ensures API calls go to the same domain's /api endpoints

# Vercel sets these via dashboard (not needed in this file):
# - SECRET_TOKEN
# - GOOGLE_SHEET_ID
# - GOOGLE_SERVICE_ACCOUNT_KEY
# - VAPID_PUBLIC_KEY
# - VAPID_PRIVATE_KEY
# - VAPID_EMAIL
# - VITE_VAPID_PUBLIC_KEY
